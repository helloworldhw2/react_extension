import React, { Component } from 'react';

class Lazy extends Component {
  render() {
    return (
      <div style={{backgroundColor:'red'}}>
        loading
      </div>
    );
  }
}

export default Lazy;